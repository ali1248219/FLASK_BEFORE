-- CREATE TABLE USERS(username TEXT NOT NULL PRIMARY KEY,password NOT NULL,email NOT NULL);

-- INSERT INTO USERS(username,password,email) VALUES ("fead", "sadasd", "email23@gmail");

-- SELECT * FROM USERS;

-- SELECT email FROM USERS WHERE username ='feadri';

-- SELECT email FROM USERS WHERE username IN ('feadri','johnson');

-- UPDATE USERS SET email='myemail@gmail.com', password='mypassword' WHERE username ='johnson';

-- DELETE FROM USERS;

-- DROP TABLE IF EXISTS USERS;

-- CREATE TABLE USERS(id INTEGER PRIMARY KEY AUTOINCREMENT , name TEXT NOT NULL, satker TEXT NOT NULL, bidang TEXT NOT NULL);

-- INSERT INTO USERS(name, satker, bidang) VALUES ('Aul' , 'Biro Pengadaan' , 'PBJ');

-- SELECT * FROM USERS;

-- UPDATE USERS SET id='2' WHERE id ='4';